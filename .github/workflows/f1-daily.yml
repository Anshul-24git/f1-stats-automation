# This workflow will run a job every day at 5:03 UTC

name: Daily Workflow

on:
  schedule:
    - cron: '5 3 * * *'

jobs:
  daily_job:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      - name: Run daily script
        run: ./path/to/script.sh