# This workflow is triggered every day at 5 AM UTC
name: Daily Trigger

on:
  schedule:
    - cron: '35 3 * * *'  # Daily at 5 AM UTC

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      - name: Run script
        run: echo 'Running daily job'